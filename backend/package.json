{
  "name": "distro-indicators-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply distro-indicators --local",
    "migrate:remote": "wrangler d1 migrations apply distro-indicators --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250909.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
